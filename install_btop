#!/bin/bash

set -e

echo "📦 Скачиваем последнюю сборку btop++..."
wget -q --show-progress https://github.com/aristocratos/btop/releases/latest/download/btop-x86_64-linux-musl.tbz

echo "📦 Распаковываем архив..."
tar -xvjf btop-x86_64-linux-musl.tbz >/dev/null

cd btop || { echo "❌ Не удалось перейти в папку btop"; exit 1; }

echo "🔧 Устанавливаем btop++..."
sudo make install
echo "✅ Установка завершена"

# Проверка версии
echo
btop --version
echo

# Создание конфига
echo "🧰 Первый запуск для генерации конфига..."
mkdir -p ~/.config/btop
timeout 2s btop >/dev/null 2>&1 || true

# ⏳ Подождать генерацию btop.conf, до 5 секунд
for i in {1..5}; do
    if [ -f ~/.config/btop/btop.conf ]; then
        break
    fi
    sleep 1
done

# Установка темы Candy
if [ -f ~/.config/btop/btop.conf ]; then
    echo "🎨 Включаем тему Candy..."
    sed -i 's/^color_theme = .*/color_theme = "Candy"/' ~/.config/btop/btop.conf
else
    echo "⚠️ Не удалось найти btop.conf, тема не установлена"
fi

# Установка темы Candy в /usr/share
if [ ! -f /usr/share/btop/themes/candy.theme ]; then
    echo "⬇️ Скачиваем тему candy..."
    sudo wget -q https://raw.githubusercontent.com/aristocratos/btop/main/themes/candy.theme -O /usr/share/btop/themes/candy.theme
fi

echo
echo "🚀 Btop++ установлен!"
echo "👉 Для запуска: btop"
echo "❌ Выход: Ctrl+C"
